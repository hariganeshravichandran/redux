import React from 'react';

function Button({ name }) {
    name='leela'
  return(

  <div>
       <button className='bgone'>
           {name}
       </button>
  </div>
  )
}

export default Button;
